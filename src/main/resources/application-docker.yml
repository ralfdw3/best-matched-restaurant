spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/bestmatchedrestaurants
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: create

logging:
  level:
    org.springframework: INFO
    io.challenge.bestmatched: DEBUG